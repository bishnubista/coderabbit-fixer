#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=bin/_cr_dispatch.sh
source "${SCRIPT_DIR}/_cr_dispatch.sh"

cr_dispatch_main "cr-gather" "$@"
